<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.util.utilitarios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImcActivity" android:label="Índice de massa corporal (IMC)"></activity>
        <activity android:name=".ConversaoActivity" android:label="Conversor de dólares para reais"></activity>
        <activity android:name=".ConversaoPendriveDisqueteActivity" android:label="Conversor pendrive em disquetes"></activity>
    </application>

</manifest>